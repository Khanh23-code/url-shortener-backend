name: URL Shortener CI-CD

on:
  push:
    [cite_start]branches: [ "main" ]

jobs:
  build-and-test:
    [cite_start]runs-on: ubuntu-latest 
    steps:
      - name: Checkout code
        [cite_start]uses: actions/checkout@v3 

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm install

      - name: Build app
        [cite_start]run: echo "Building the application..."